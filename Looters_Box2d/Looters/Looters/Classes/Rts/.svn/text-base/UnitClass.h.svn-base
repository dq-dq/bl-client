#pragma once
#include "cocos2d.h"
using namespace cocos2d;
using namespace std;

class UnitClass {
public:
    int     ID;                 // 对象ID
    
    int     speed;              // 速度
    int     camp;               // 阵营
    int     hp;                 // 初始血量
    int     maxhp;              // 最大血量
    int     offsetx;            // 建筑的偏移x
    int     offsety;            // 建筑的偏移y
    int     radius;             // 建筑下方的触碰半径
    int     height;             // 建筑的高
    bool    hideHpbar;          // 是否隐藏血条
    int     action;             // 初始actionID
    
    int     lv;                 // 等级
    int     TypeID;             // 类别
    int     Damage;             // 伤害值
    float   AttackRange;        // 攻击范围半径
    float   MinAttackRange;     // 盲区范围
    int     DamageRadius;       // 攻击的溅射范围
	int     AttackSpeed;        // 攻击的CD时间
    bool    AirTargets;         // 是否对空
    bool    GroundTargets;      // 是否对地
    bool    IsFlying;           // 判定是否为空中单位
    
    int     MaxStoredGold;      // 存储金币上限
    int     MaxStoredElixir;    // 存储水上限
    int     ResourceMax;        // 生产后存储资源的上限
    
    // characters
    int     PreferedTargetBuilding; //优先攻击的建筑类别
    
    UnitClass()
    {
        
    }
};